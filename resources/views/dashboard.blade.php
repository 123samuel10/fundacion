            {{-- @if(Auth::user()->usertype === 'admin')
                <!-- Aquí puedes poner el contenido específico para el admin -->
                <x-app-layout>
                    <x-slot name="header">
                        <h2 class="font-semibold text-xl text-white dark:text-gray-200 leading-tight">
                            {{ __('Dashboard') }}
                        </h2>
                    </x-slot>
                </x-app-layout>
            @else
                <h1>Bienvenido Usuario</h1>
                <!-- Aquí puedes poner el contenido específico para el usuario -->
                <x-app-layout>
                </x-app-layout>
            @endif
 --}}

{{-- <h1>admin</h1> --}}
 {{-- <x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-white dark:text-gray-200 leading-tight">
            {{ __('Dashboard') }}
        </h2>
    </x-slot>
</x-app-layout> --}}
